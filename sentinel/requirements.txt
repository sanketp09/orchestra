# Orchestra Sentinel — pinned dependencies
# LLM provider: Google Gemini (free tier) — replaces Anthropic + OpenAI
# Get free API key: https://aistudio.google.com/app/apikey

fastapi==0.141.1
uvicorn[standard]==0.52.0

# LangGraph orchestration (Feature 1: AI Site Walk)
langgraph==1.2.10
langchain==1.3.14
langchain-google-genai==2.0.10   # LangChain adapter for Gemini (used by LangGraph)

# Google Gemini SDK — vision + embeddings (free tier)
google-generativeai==0.8.4
Pillow==11.1.0                   # PIL for image conversion before Gemini vision calls

# Database
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
pgvector==0.5.0

# Video processing (Feature 1)
opencv-python==4.13.0.92

# Core
pydantic==2.13.4
python-multipart==0.0.32         # multipart file uploads (video upload endpoint)

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
